{% assign person = personal_detail %}
<table width="100%">
  <tr>
    <td align=center>
      <h1>{{ person.name }}</h1>
      <h4>
        {{ person.address1 }}, {{ person.address2 }}, {{ person.address3 }}
      </h4>
      <h4>
        {{ person.email }}
        {{ person.website }} 
        {{ person.phone }} 
        {{ person.fax }} 
      </h4>
    </td>
  </tr>
  <tr>
  </tr>
</table>
