<h2>{{ section.name }}</h2>
{% if section.ordered_list %}
  {% assign list_type = 'ordered'   %}
{% else %}
  {% assign list_type = 'unordered' %}
{% endif %}

<ul class="{{ list_type }}">
  {% for item in section.items %}
    <li>{{ item.content }}</li>
  {% endfor %}
</ul>
