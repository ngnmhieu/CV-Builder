<!DOCTYPE html>
<html>
<head>
  <title><%= @resume.name %></title>
  <%= liquidize(@template_content[:head], @data).html_safe %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= liquidize(@template_content[:body], @data).html_safe %>
</body>
</html>
