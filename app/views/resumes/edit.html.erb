<%= form_for @resume do |r| %>
  <%= r.label :name, 'Resume Name: '%>
  <%= r.text_field :name %>
  <hr />
   
  <%= render(partial: 'personal_detail_form', locals: {rform: r}) %>
  <hr />

  <%# <%= render(partial: 'education_form', locals: {rform: r}) %>
  <!-- <hr /> -->

  <%# <%= render(partial: 'work_form', locals: {rform: r}) %>
  <!-- <hr /> -->

  <%= render(partial: 'simplelist_form', locals: {rform: r}) %>
  <hr />

  <%= render(partial: 'multiline_list_form', locals: {rform: r}) %>
  <br />

  <%= render(partial: 'textsection_form', locals: {rform: r}) %>
  <br />

  <br />
  <%= r.submit 'Save' %>
<% end %>
